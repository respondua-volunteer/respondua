apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "respondua.fullname" . }}-test-connection"
  labels:
    {{- include "respondua.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "respondua.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
