apiVersion: networking.istio.io/v1beta1
kind: ServiceEntry
metadata:
  name: grafana-external
spec:
  hosts:
  - grafana.com
  - grafana.net
  ports:
  - number: 443
    name: https
    protocol: HTTPS
  - number: 80
    name: http
    protocol: HTTP
  location: MESH_EXTERNAL
  resolution: DNS
