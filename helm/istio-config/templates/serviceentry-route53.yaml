apiVersion: networking.istio.io/v1beta1
kind: ServiceEntry
metadata:
  name: route53-amazonaws
  namespace: cert-manager
spec:
  hosts:
    - route53.amazonaws.com
  location: MESH_EXTERNAL
  resolution: DNS
  ports:
    - number: 443
      name: https
      protocol: HTTPS
